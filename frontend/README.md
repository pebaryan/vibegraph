# GraphDB Frontend

This is an Angular application that provides a user interface for interacting with the GraphDB backend.

## Setup

```bash
cd frontend
npm install
ng serve
```

Open `http://localhost:4200` to use the application.
